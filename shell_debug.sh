#! /bin/bash -x
# -x in the previous line does the same debugging as explained below

# Use bash -x shell_debug.sh to run the script in debug mode from
# console


# OR use `set -x` at the line from where you want to start debugging
# use `set +x` to stop the debugging from that line onwards

